test_table_path: "data/auction+verification/test_data.csv" # Relative path (to repo root) of test dataset (i.e. csv table)
model_path: "output/model.pth" # Relative path (to repo root) of pretrained model used for inference and evaluation
batch_size: 16
shuffle: False
plot_title: "y_pred_vs_real_test"
